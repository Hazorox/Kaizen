import React from 'react'
interface card {
    text:string,
    furigana:string,
    definitions:string[],
    samples:React.ReactElement[],
}
const WordCard = () => {
  return (
    <div>WordCard</div>
  )
}

export default WordCard